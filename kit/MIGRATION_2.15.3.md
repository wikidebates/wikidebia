# Migration vers le kit 2.15.3

Le rendu anglais localise désormais tous les paramètres du modèle `Quote`. Les workspaces rendus sous 1.2.27 à 1.2.29 doivent être repris à partir de la traduction verrouillée puis rendus de nouveau. Les anciennes copies rendues contenant `citation`, `auteurs`, `ouvrage` ou tout autre paramètre français dans `Quote` ne doivent pas être publiées.
