# Migration vers le kit 2.16.44

Aucune migration du corpus ou de `.state/` n’est requise. Après `upgrade`, relancer la même commande `./wikidebia workflow` sur un Work bloqué par `fingerprint_path`. Les plans finaux peuvent être reconstruits tant qu’aucune autorisation/écriture distante n’a commencé ; la baseline, la convergence et le checkpoint français restent inchangés.
