# Audit de régression kit 2.16.2

Le correctif est limité au calcul et à la reprise du `short_code`. Les tests ajoutés couvrent la dérivation ASCII, `revenu_de_base` → `RDB`, le corpus-init direct, la reprise automatique d’un workflow incomplet et le refus d’un code contradictoire. Toutes les primitives avancées et protections de publication restent inchangées.
